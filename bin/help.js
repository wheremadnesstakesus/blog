module.exports = `
	Usage
	  $ blog [title] [options]

	Options
    --draft, -d  Create a draft post
    --featured, -f Create a featured post
    --hero, -i Add hero image for the post
    --keywords, -k Add keywords for SEO
    --summary, -s Add a sumary
    --tags, -t Add tags

	Examples
	  $ blog our new blog post! --draft -f --keywords newpost title --tags category vanlife -s "A small summary for this post"
`
